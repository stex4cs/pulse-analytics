user  nginx;
worker_processes  auto;
worker_rlimit_nofile 65535;

events {
    worker_connections  4096;
    multi_accept on;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    sendfile      on;
    tcp_nopush    on;
    tcp_nodelay   on;
    keepalive_timeout  65;
    server_tokens off;

    log_format pulse '$remote_addr - $status $request_time "$request" "$http_user_agent"';
    access_log /var/log/nginx/access.log pulse buffer=32k flush=5s;

    gzip on;
    gzip_types text/plain text/css application/javascript application/json;
    gzip_min_length 512;

    # Peak tokom derbija: 2000 req/s burst (sekcija 4.3).
    # Ovo je zastita infrastrukture; fina granulacija po IP-u je u aplikaciji.
    limit_req_zone $binary_remote_addr zone=collect:20m rate=120r/m;
    limit_req_zone $binary_remote_addr zone=api:10m     rate=600r/m;
    limit_req_status 429;

    # Ingest zadrzava upstream blok: na 2000 req/s keepalive i least_conn
    # vrede vise od automatskog re-resolve-a. Posle redeploy-a ingest-a
    # pokrenuti `docker compose restart nginx` (vidi operativni prirucnik).
    upstream pulse_ingest {
        least_conn;
        server ingest-1:8080 max_fails=2 fail_timeout=10s;
        server ingest-2:8080 max_fails=2 fail_timeout=10s;
        keepalive 64;
    }

    # Docker-ov ugradjeni DNS. nginx OSS razresava `upstream` blokove samo pri
    # startu, pa svaki redeploy kontejnera menja IP i ostavlja nginx na starom -
    # 502 do rucnog restarta. Za API i dashboard koristimo promenljivu u
    # proxy_pass, sto tera ponovno razresavanje u toku rada.
    resolver 127.0.0.11 valid=10s ipv6=off;

    # ── Ingestion + SDK ──────────────────────────────────────────────────────
    server {
        listen 80;
        server_name pulse.tvarenasport.com;

        # U produkciji: redirect na 443 i TLS blok ispod
        # return 301 https://$host$request_uri;

        add_header X-Content-Type-Options "nosniff" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;

        location /pulse.js {
            alias /usr/share/nginx/html/sdk/pulse.js;
            add_header Cache-Control "public, max-age=3600, stale-while-revalidate=86400";
            add_header Access-Control-Allow-Origin "*";
            gzip_static on;
        }

        location /collect {
            limit_req zone=collect burst=200 nodelay;

            proxy_pass http://pulse_ingest;
            proxy_http_version 1.1;
            proxy_set_header Connection "";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # Ingest uvek odgovara 204 - ne cekamo dugo
            proxy_connect_timeout 2s;
            proxy_read_timeout 5s;
        }

        location /ab/ {
            proxy_pass http://pulse_ingest;
            proxy_http_version 1.1;
            proxy_set_header Connection "";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_read_timeout 3s;
        }

        location /health {
            proxy_pass http://pulse_ingest;
            access_log off;
        }

        # Metrike se ne izlazu javno
        location /metrics { deny all; }
    }

    # ── Dashboard + API ──────────────────────────────────────────────────────
    server {
        listen 80;
        server_name analitika.tvarenasport.com;

        # Sigurnosna zaglavlja. Dashboard je interni alat - nema razloga da se
        # ugradjuje u iframe, njuska MIME tip ili salje referrer napolje.
        add_header X-Frame-Options "DENY" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;

        location /api/ {
            limit_req zone=api burst=100 nodelay;

            set $pulse_api api:8081;
            proxy_pass http://$pulse_api;
            proxy_http_version 1.1;
            proxy_set_header Connection "";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_read_timeout 60s;
        }

        location / {
            set $pulse_dashboard dashboard:3000;
            proxy_pass http://$pulse_dashboard;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        location /metrics { deny all; }
    }

    # ── TLS (ukljuciti kad su sertifikati na mestu) ──────────────────────────
    # server {
    #     listen 443 ssl http2;
    #     server_name pulse.tvarenasport.com;
    #     ssl_certificate     /etc/nginx/certs/fullchain.pem;
    #     ssl_certificate_key /etc/nginx/certs/privkey.pem;
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers HIGH:!aNULL:!MD5;
    #     ssl_session_cache shared:SSL:10m;
    #     add_header Strict-Transport-Security "max-age=31536000" always;
    #     ... iste location sekcije kao gore ...
    # }
}
